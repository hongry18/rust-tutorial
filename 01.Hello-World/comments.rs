fn main() {
    // This is an example of a line comment

    /*
     * This is another type of comment, a block comment.
     */

    /*
    Note: The previous column of `*` was entriely for style. There's no actual need for it.
    */

    let x = 5 + /* 90 + */ 5;
    println!("Is 'x' 10 or 100? x = {}", x)
}
